# Швидкий старт

## Покращена система інвентаризації v2.0

### Що нового?

- ✅ Підтримка .env файлів для безпеки
- ✅ Автоматичні міграції БД (Flask-Migrate)
- ✅ Резервне копіювання (автоматичне + ручне)
- ✅ Нагадування про обслуговування пристроїв
- ✅ REST API для інтеграцій
- ✅ Експорт в PDF
- ✅ Unit та Integration тести

---

## Встановлення (5 хвилин)

### 1. Клонуйте репозиторій

```bash
cd /path/to/inventory
```

### 2. Встановіть залежності

```bash
# Активуйте віртуальне середовище (якщо потрібно)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# або
venv\Scripts\activate  # Windows

# Встановіть пакети
pip install -r requirements.txt
```

### 3. Налаштуйте .env

```bash
# Створіть .env з прикладу
cp env.example .env
```

Відредагуйте `.env` та встановіть SECRET_KEY:

```bash
# Згенеруйте безпечний ключ
python -c "import secrets; print(secrets.token_hex(32))"
```

Вставте згенерований ключ у `.env`:

```env
SECRET_KEY=your-generated-key-here
BACKUP_AUTO_ENABLED=true
```

### 4. Ініціалізуйте БД

```bash
# Створіть таблиці
python app.py
# Натисніть Ctrl+C після появи "Running on http://127.0.0.1:5000"

# Ініціалізуйте міграції (для майбутніх оновлень)
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

### 5. Запустіть сервер

```bash
python app.py
```

Відкрийте браузер: **http://localhost:5000**

---

## Перший вхід

**Логін:** `admin`  
**Пароль:** `admin`

⚠️ **Важливо:** Змініть пароль після першого входу!

---

## Основні функції

### 📦 Управління пристроями

1. **Додати пристрій:** Головна → Пристрої → Додати пристрій
2. **Імпорт з Excel:** Пристрої → Імпорт з Excel
3. **Експорт в PDF:** Оберіть пристрої → Експорт в PDF
4. **QR коди:** Відкрийте пристрій → Друк QR коду

### 💾 Резервне копіювання

1. **Ручний backup:** Адмін → Резервне копіювання → Створити
2. **Автоматичний:** Працює щодня о 2:00 (якщо BACKUP_AUTO_ENABLED=true)
3. **Завантажити backup:** Адмін → Резервне копіювання → Завантажити

### 🔔 Нагадування

- Автоматична перевірка щодня о 9:00
- Нагадування про прострочені обслуговування (червоний)
- Попередження про майбутні (жовтий)

### 🌐 REST API

**Приклад запиту:**

```bash
curl -X GET "http://localhost:5000/api/v1/devices" \
  -H "X-API-Key: admin"
```

Докладніше: `API_DOCUMENTATION.md`

---

## Тестування

Запустіть тести для перевірки:

```bash
python run_tests.py
```

Очікувана відповідь: **OK (23 tests passed)**

---

## Адміністрування

### Створення користувача

1. Увійдіть як admin
2. Адмін → Користувачі → Додати користувача
3. Вкажіть username, пароль, місто

### Створення міста

1. Адмін → Міста → Додати місто
2. Вкажіть назву міста

### Журнал активності

1. Адмін → Активність користувачів
2. Перегляд всіх дій з IP адресами

---

## Планувальник задач

Автоматичні завдання (працюють у фоні):

| Завдання | Час | Опис |
|----------|-----|------|
| Backup БД | 2:00 щодня | Створення резервної копії |
| Очищення backup | Неділя, 3:00 | Видалення старих копій (>30 днів) |
| Перевірка обслуговування | 9:00 щодня | Створення нагадувань |

---

## Структура проекту

```
inventory/
├── app.py                 # Головний файл
├── config.py              # Конфігурація
├── models.py              # Моделі БД
├── utils.py               # Допоміжні функції
├── utils_pdf.py           # Генерація PDF
├── blueprints/            # Модулі
│   ├── auth.py           # Автентифікація
│   ├── devices.py        # Пристрої
│   ├── admin.py          # Адмін-панель
│   └── api.py            # REST API
├── templates/             # HTML шаблони
├── static/                # Статичні файли
├── tests/                 # Тести
├── backups/               # Резервні копії
└── uploads/               # Завантажені файли
```

---

## Корисні команди

### Міграції БД

```bash
# Створити міграцію після зміни моделей
flask db migrate -m "Опис змін"

# Застосувати міграції
flask db upgrade

# Відкотити останню міграцію
flask db downgrade
```

### Backup

```bash
# Ручний backup через Python
flask shell
>>> from utils import backup_database
>>> backup_database('backups')
>>> exit()
```

### Очищення

```bash
# Видалити старі backup
flask shell
>>> from utils import cleanup_old_backups
>>> cleanup_old_backups('backups', keep_days=30)
>>> exit()
```

---

## Усунення проблем

### Помилка "No module named 'flask_migrate'"

```bash
pip install Flask-Migrate
```

### Помилка "SECRET_KEY not set"

Перевірте, що `.env` файл існує і містить SECRET_KEY.

### Планувальник не запускається

Перевірте логи при запуску. Має бути:
```
Планувальник задач запущено
```

### Помилка при створенні backup

Перевірте, що папка `backups` існує і має права на запис:

```bash
mkdir -p backups
chmod 755 backups  # Linux/Mac
```

---

## Оновлення з попередньої версії

Якщо у вас вже є стара версія:

1. **Зробіть backup БД:**
   ```bash
   cp instance/inventory_dev.db instance/inventory_dev.db.backup
   ```

2. **Оновіть код:**
   ```bash
   git pull
   ```

3. **Встановіть нові залежності:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Застосуйте міграції:**
   ```bash
   flask db upgrade
   ```

5. **Перезапустіть сервер:**
   ```bash
   python app.py
   ```

Докладніше: `UPGRADE_GUIDE.md`

---

## Документація

- **UPGRADE_GUIDE.md** - посібник оновлення
- **API_DOCUMENTATION.md** - REST API документація
- **IMPROVEMENTS_SUMMARY.md** - список всіх змін
- **README.md** - повна документація

---

## Підтримка

**Email:** [ваш email]  
**Issue Tracker:** [GitHub Issues]

---

## Ліцензія

[Вкажіть вашу ліцензію]

---

**Версія:** 2.0  
**Дата:** Жовтень 2025  
**Статус:** ✅ Production Ready

